%p=[11.0/13.0, 1/2, 7/26, 0];
%q=[0.5,0.5,0.5,0.5];

for p1=0:1
    for p2=0:1
        for p3=0:1
            for p4=0:1
                for q1=0:1
                    for q2=0:1
                        for q3=0:1
                            for q4=0:1

p=[p1,p2,p3,p4];
q=[q1,q2,q3,q4];
% p=[0,0,0,0];
% q=[0,0,0,0];

% p1=p(1);
% p2=p(2);
% p3=p(3);
% p4=p(4);
% q1=q(1);
% q2=q(2);
% q3=q(3);
% q4=q(4);

% disp((p1*p3-p1+p1*p2)*q1+(p3-p1*p3)*q2+(p2-p1*p2)*q3-p3-p2+1); 

%calculate D,D2,D3
D  =  [p(1)-1, p(2)-1, p(3), p(4);
q(1)-1, q(3), q(2)-1, q(4); 
p(1)*q(1)-1, p(2)*q(3), p(3)*q(2), p(4)*q(4);
1, 1, 1, 1];
    
D1 = D;
D2 = D;
D3 = D;
D4 = D;
    
D1(:,1) = [0;0;0;1];
D2(:,2) = [0;0;0;1];
D3(:,3) = [0;0;0;1];
D4(:,4) = [0;0;0;1];

if (det(D)>0)
    disp(det(D));
end;
% disp(det(D2));
% disp(det(D3));

%disp((p3*q4 - p3*q2*q4 - p1*p3*q1*q4 + p1*p3*q2*q4)^2 - (q4 - p2*q4 - p1*q1*q4 + p2*q3*q4 + p1*p2*q1*q4 - p1*p2*q3*q4)^2);
                            end;
                        end;
                    end;
                end;
            end;
        end;
    end;
end;